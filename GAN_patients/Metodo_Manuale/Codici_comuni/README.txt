la cartella Codici_comuni contiene tutti i codici che sono identici per il training della rete su qualsiasi paziente:
--train_audio_Q.pu contiene il codice del training della rete 
--DCGAN_dys.py contiene l'architettura del G e del D 
--utils_GAN.py contiene alcune funzioni utilizzate nei codici come quella per importare il dataset
--losses.py contiene alcune loss utilizzate nel training 

